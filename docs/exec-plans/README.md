# Execution plans

Working plans for multi-step changes live here while in flight and move to `completed/` when done.

- `active/` — plans currently being executed
- `completed/` — finished plans kept for archaeology

A plan is a Markdown file with goal, ordered steps, and verification per step. Delete nothing; completed plans document why the code looks the way it does.
